---
interface Props {
  href: string;
  label: string;
  class?: string;
}

const { href, label, class: className } = Astro.props;
---

<a class:list={["gateway-link", "gateway-link--muted", className]} href={href}>
  <span class="gateway-link__label">{label}</span>
</a>
